{
  "name": "@shranix/desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "typecheck": "echo 'Typecheck requires Rust toolchain - run locally with Rust'",
    "clean": "node -e \"fs.rmSync('.turbo',{recursive:true,force:true});try{fs.rmSync('src-tauri/target',{recursive:true,force:true})}catch(e){}\""
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "esbuild": "^0.28.2"
  }
}
